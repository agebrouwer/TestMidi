<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="TestMidi.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25"
            VerticalOptions="Center">


            <Label
                x:Name="ConnectedInputDevices"
                FontSize="32"
                HorizontalOptions="Center"
                Text="No ouput devices conected" />

            <Label
                x:Name="ConnectedOutputDevices"
                FontSize="32"
                HorizontalOptions="Center"
                Text="No ouput devices conected" />


        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
